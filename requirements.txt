# EEGPT pretraining — Python dependencies (training only)
# Tested on Ubuntu 20.04, CUDA 11.8, NVIDIA driver 515.65.01, RTX 3090
# Python 3.11 (any 3.10+ should work)
#
# Install:  pip install -r requirements.txt
#
# IMPORTANT: pytorch-lightning >= 2.0 is REQUIRED (code uses precision="bf16-mixed").
#            PL 1.x does NOT accept this string and will crash.
#
# For preprocessing (preprocess_tuh.py / preprocess_moabb.py), additionally install
# mne + moabb — see requirements-preprocess.txt. moabb 1.2.0 needs numpy<2, which
# conflicts with this file's numpy>=2, so use a SEPARATE conda env for preprocessing.

--extra-index-url https://download.pytorch.org/whl/cu118

# === Core training ===
torch==2.6.0+cu118
torchvision==0.21.0+cu118
torchaudio==2.6.0+cu118
pytorch-lightning==2.6.5          # code uses precision="bf16-mixed" (needs PL >= 2.0)
torchmetrics==1.9.0
numpy==2.4.3
pandas==3.0.3
scipy==1.17.1
einops==0.8.2
tqdm==4.67.3

# === Logging ===
tensorboardX==2.6.5               # PL's TensorBoardLogger uses this; or install `tensorboard`
